package enum

const (
	// Authorization - Label of the header which contains the authentication token.
	Authorization string = "Authorization"
	// AcceptLanguage - Label of the header which contains the language code of the requester.
	AcceptLanguage string = "Accept-Language"
)
